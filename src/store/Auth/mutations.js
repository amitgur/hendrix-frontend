export function setSignIn(state, status) {
  state.signIn = status;
}

export function setName(state, name) {
  state.name = name;
}

export function setProfile(state, profile) {
  state.profile = profile;
}
